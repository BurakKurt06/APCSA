//Burak Kurt
public class TenToAnyRunner
{
	public static void main( String args[] )
	{
		TenToAny one = new TenToAny(0, 0);
		System.out.println(one.base10 + " base 10 is " + one.getNewNum() + " base " + one.newBase);	
		}
	}